CREATE TABLE t_products
(
    id          bigint NOT NULL,
    description varchar(255),
    image_url   varchar(255),
    name        varchar(255),
    price       float,
    rating      float,
    size_type   varchar(255)
);